//Edimilson Baltazar Baloi Eddy@Emb 2k18
import java.util.Scanner;
//Imprime o resultado da Subtraccao de valores de uma String
public class TestaSubtracacaoString{
	public static void main(String[] args) {
		//Operadores
		String sinais="+" || "-" || "*" || "x" || "X" || "/" || "(" || ")" ;
		//String Dados
		String operacao="14-98";
			//numero1 14
			int numero1=Integer.parse(SubString(0,equacao.indexOf("-")));
			//numero2 98
			int numero2=Integer.parse(SubString(equacao.indexOf("-")+1,equacao.indexOf("=")));
			//Resultado 10-98
			double resultado=numero1-numero2;
		System.out.println("O resultado de "+operacao+" e' "+resultado);
	}		
}
		if (operacao.contains(sinais)) {
				for (int i=0;i<operacao.length();i++) {
					contasinal=0;
				if(charAt(i)==sinais){
					;
				}
			}



				int sinal[]=new int[contasinal];
				for (int i=0;i<operacao.length();i++) {
					contasinal=0;
				if(sinal[i]==sinais){
					contasinal++;
				}
			}


		}






















	if (operacao.contains(sinais)) {
	int pnum=Integer.parse(SubString(0,operacao.indexOf(sinais)));
	int snum=Integer.parse(SubString(operacao.indexOf(sinais)));
	}

}